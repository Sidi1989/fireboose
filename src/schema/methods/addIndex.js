/**
 * @description Add a new index into the array of indexes,
 * which is a property in the Schema constructor
 * @param {*} index 
 */
const addIndex = function (index) {
  this.indexes.push(index)
};




export default addIndex;